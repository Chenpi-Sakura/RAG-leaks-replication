# =================================================================
# RAG-leaks 复现 - 依赖清单
# -----------------------------------------------------------------
# ⚠️ 安装顺序：先装 vllm（拉它兼容的 torch/transformers），
#             再装其他（适配已装的 torch）。不要重排！
#   pip install vllm
#   pip install -r requirements.txt --no-deps
# -----------------------------------------------------------------
# 不锁版本号：vllm 决定 torch/transformers 版本，其他包适配。
# =================================================================

# LLM 客户端
openai

# 本地推理（vllm 自动拉 torch / transformers；不要重复列）
vllm

# 嵌入 / 检索
sentence-transformers
faiss-cpu
rank-bm25
Pillow

# 工具
python-dotenv
tenacity
huggingface-hub
numpy
scipy
scikit-learn
